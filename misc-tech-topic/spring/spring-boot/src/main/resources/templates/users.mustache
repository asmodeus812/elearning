<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Users</title>
        <style>
            :root {
                --bg: #0b1020;
                --panel: rgba(255, 255, 255, 0.06);
                --panel2: rgba(255, 255, 255, 0.08);
                --border: rgba(255, 255, 255, 0.12);
                --text: rgba(255, 255, 255, 0.92);
                --muted: rgba(255, 255, 255, 0.66);
                --muted2: rgba(255, 255, 255, 0.48);
                --shadow: 0 14px 40px rgba(0, 0, 0, 0.35);
                --radius: 18px;
            }

            * {
                box-sizing: border-box;
            }
            body {
                margin: 0;
                font-family:
                    ui-sans-serif,
                    system-ui,
                    -apple-system,
                    Segoe UI,
                    Roboto,
                    Helvetica,
                    Arial,
                    "Apple Color Emoji",
                    "Segoe UI Emoji";
                color: var(--text);
                background:
                    radial-gradient(1200px 700px at 20% -10%, rgba(99, 102, 241, 0.35), transparent 60%),
                    radial-gradient(900px 600px at 90% 10%, rgba(34, 197, 94, 0.22), transparent 55%),
                    radial-gradient(800px 500px at 50% 120%, rgba(245, 158, 11, 0.18), transparent 60%), var(--bg);
                min-height: 100vh;
                padding: 36px 16px 64px;
            }

            .container {
                max-width: 1100px;
                margin: 0 auto;
            }

            header {
                display: flex;
                align-items: flex-end;
                justify-content: space-between;
                gap: 16px;
                margin-bottom: 18px;
            }

            h1 {
                margin: 0;
                font-size: clamp(24px, 3vw, 34px);
                letter-spacing: -0.02em;
                line-height: 1.1;
            }

            .subtitle {
                margin: 8px 0 0;
                color: var(--muted);
                font-size: 14px;
            }

            .grid {
                display: grid;
                grid-template-columns: 1.2fr 0.8fr;
                gap: 18px;
                align-items: start;
            }
            @media (max-width: 980px) {
                .grid {
                    grid-template-columns: 1fr;
                }
            }

            .card {
                background: linear-gradient(180deg, var(--panel), rgba(255, 255, 255, 0.03));
                border: 1px solid var(--border);
                border-radius: var(--radius);
                box-shadow: var(--shadow);
                overflow: hidden;
                backdrop-filter: blur(10px);
            }

            .card-header {
                padding: 16px 18px;
                display: flex;
                align-items: center;
                justify-content: space-between;
                border-bottom: 1px solid var(--border);
                background: rgba(255, 255, 255, 0.03);
            }

            .card-header h2 {
                margin: 0;
                font-size: 16px;
                letter-spacing: 0.02em;
                color: var(--text);
            }

            .badge {
                font-size: 12px;
                color: var(--muted);
                padding: 6px 10px;
                border: 1px solid var(--border);
                border-radius: 999px;
                background: rgba(255, 255, 255, 0.04);
            }

            /* List */
            ul.users {
                list-style: none;
                margin: 0;
                padding: 10px;
                display: flex;
                flex-direction: column;
                gap: 10px;
            }

            .user-item {
                border: 1px solid var(--border);
                border-radius: 14px;
                padding: 12px;
                background: rgba(255, 255, 255, 0.04);
                transition:
                    transform 0.12s ease,
                    background 0.12s ease,
                    border-color 0.12s ease;
            }
            .user-item:hover {
                transform: translateY(-1px);
                background: rgba(255, 255, 255, 0.06);
                border-color: rgba(255, 255, 255, 0.18);
            }

            .user-top {
                display: flex;
                gap: 12px;
                align-items: flex-start;
                justify-content: space-between;
                margin-bottom: 10px;
                flex-wrap: wrap;
            }

            .meta {
                display: flex;
                gap: 10px;
                align-items: flex-start;
                min-width: 0;
                flex: 1 1 auto;
            }

            .id-pill {
                flex: 0 0 auto;
                font-size: 12px;
                color: var(--muted);
                padding: 6px 10px;
                border-radius: 999px;
                border: 1px solid var(--border);
                background: rgba(0, 0, 0, 0.15);
            }

            .title-wrap {
                min-width: 0;
                flex: 1 1 auto;
            }
            .title {
                margin: 0;
                font-size: 15px;
                font-weight: 700;
                letter-spacing: -0.01em;
                white-space: nowrap;
                overflow: hidden;
                text-overflow: ellipsis;
            }
            .desc {
                margin: 4px 0 0;
                font-size: 13px;
                color: var(--muted);
                line-height: 1.35;
            }

            .chips {
                display: flex;
                flex-wrap: wrap;
                gap: 8px;
                margin-top: 10px;
            }

            .chip {
                font-size: 12px;
                color: var(--muted);
                padding: 6px 10px;
                border: 1px solid var(--border);
                border-radius: 999px;
                background: rgba(255, 255, 255, 0.04);
                max-width: 100%;
                white-space: nowrap;
                overflow: hidden;
                text-overflow: ellipsis;
            }
            .chip strong {
                color: var(--text);
                font-weight: 700;
            }

            code {
                padding: 2px 6px;
                border-radius: 8px;
                background: rgba(0, 0, 0, 0.22);
                border: 1px solid rgba(255, 255, 255, 0.1);
                color: rgba(255, 255, 255, 0.9);
            }

            .actions {
                display: flex;
                gap: 10px;
                justify-content: flex-end;
                align-items: center;
                flex-wrap: wrap;
            }

            button,
            a.btn {
                border: 1px solid rgba(255, 255, 255, 0.16);
                background: rgba(255, 255, 255, 0.06);
                color: var(--text);
                padding: 10px 12px;
                border-radius: 12px;
                cursor: pointer;
                transition:
                    transform 0.1s ease,
                    background 0.12s ease,
                    border-color 0.12s ease;
                font-weight: 600;
                letter-spacing: 0.01em;
                white-space: nowrap;
                text-decoration: none;
                display: inline-block;
            }
            button:hover,
            a.btn:hover {
                transform: translateY(-1px);
                background: rgba(255, 255, 255, 0.1);
                border-color: rgba(255, 255, 255, 0.22);
            }
            button:active,
            a.btn:active {
                transform: translateY(0px);
            }

            .btn-primary {
                background: linear-gradient(180deg, rgba(99, 102, 241, 0.75), rgba(99, 102, 241, 0.35));
                border-color: rgba(99, 102, 241, 0.65);
            }
            .btn-primary:hover {
                background: linear-gradient(180deg, rgba(99, 102, 241, 0.86), rgba(99, 102, 241, 0.42));
                border-color: rgba(99, 102, 241, 0.85);
            }

            .btn-danger {
                background: linear-gradient(180deg, rgba(239, 68, 68, 0.7), rgba(239, 68, 68, 0.28));
                border-color: rgba(239, 68, 68, 0.65);
            }
            .btn-danger:hover {
                background: linear-gradient(180deg, rgba(239, 68, 68, 0.82), rgba(239, 68, 68, 0.36));
                border-color: rgba(239, 68, 68, 0.85);
            }

            .inline {
                display: inline;
                margin: 0;
            }

            .pad {
                padding: 16px;
            }
            .hint {
                margin: 10px 0 0;
                color: var(--muted);
                font-size: 13px;
                line-height: 1.45;
            }
            .footer-note {
                margin-top: 12px;
                color: var(--muted2);
                font-size: 12px;
            }
        </style>
    </head>

    <body>
        <div class="container">
            <header>
                <div>
                    <h1>Manage users</h1>
                    <p class="subtitle">Manage and list existing users</p>
                </div>
            </header>

            <div class="grid">
                <section class="card">
                    <div class="card-header">
                        <h2>List of users</h2>
                    </div>

                    <ul class="users">
                        {{#users}}
                        <li class="user-item">
                            <div class="user-top">
                                <div class="meta">
                                    <div class="title-wrap">
                                        <p class="title">{{ username }}</p>
                                        <p class="desc">
                                            Role: <strong>{{ role.name }}</strong>
                                        </p>
                                    </div>
                                </div>

                                <div class="actions">
                                    <a class="btn btn-primary" href="/ui/user/{{ id }}">Edit user</a>
                                    <form action="/ui/delete-user/{{ id }}" method="post" class="inline">
                                        <input type="hidden" name="{{ _csrf.parameterName }}" value="{{ _csrf.token }}" />
                                        <button class="btn-danger" type="submit">Delete</button>
                                    </form>
                                </div>
                            </div>

                            <div class="chips">
                                {{#role.authorities}}
                                <span class="chip"
                                    ><code>{{ grant }}</code></span
                                >
                                {{/role.authorities}}

                                {{^role.authorities}}
                                <span class="chip">(no authorities)</span>
                                {{/role.authorities}}
                            </div>
                        </li>
                        {{/users}}

                        {{^users}}
                        <li class="user-item">
                            <p class="desc" style="margin: 0">No users found.</p>
                        </li>
                        {{/users}}
                    </ul>
                </section>

                <!-- RIGHT: INFO -->
                <aside class="card">
                    <div class="card-header">
                        <h2>User actions</h2>
                        <span class="badge">Info</span>
                    </div>

                    <div class="pad">
                        <p class="hint">
                            “Edit” to <code>change details</code> <br />
                            “Delete” to <code>delete user</code>
                        </p>
                        <div class="footer-note">Pure HTML + CSS, no JS.</div>
                    </div>
                </aside>
            </div>
        </div>
    </body>
</html>
