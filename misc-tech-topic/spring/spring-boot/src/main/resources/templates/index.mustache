<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Home</title>
        <style>
            :root {
                --bg: #0b1020;
                --panel: rgba(255, 255, 255, 0.06);
                --panel2: rgba(255, 255, 255, 0.08);
                --border: rgba(255, 255, 255, 0.12);
                --text: rgba(255, 255, 255, 0.92);
                --muted: rgba(255, 255, 255, 0.66);
                --muted2: rgba(255, 255, 255, 0.48);
                --shadow: 0 14px 40px rgba(0, 0, 0, 0.35);
                --radius: 18px;
            }

            * {
                box-sizing: border-box;
            }

            body {
                margin: 0;
                font-family:
                    ui-sans-serif,
                    system-ui,
                    -apple-system,
                    Segoe UI,
                    Roboto,
                    Helvetica,
                    Arial,
                    "Apple Color Emoji",
                    "Segoe UI Emoji";
                color: var(--text);
                background:
                    radial-gradient(1200px 700px at 20% -10%, rgba(99, 102, 241, 0.35), transparent 60%),
                    radial-gradient(900px 600px at 90% 10%, rgba(34, 197, 94, 0.22), transparent 55%),
                    radial-gradient(800px 500px at 50% 120%, rgba(245, 158, 11, 0.18), transparent 60%), var(--bg);
                min-height: 100vh;
                padding: 36px 16px 64px;
            }

            .container {
                max-width: 1100px;
                margin: 0 auto;
            }

            header {
                display: flex;
                align-items: flex-end;
                justify-content: space-between;
                gap: 16px;
                margin-bottom: 18px;
            }

            h1 {
                margin: 0;
                font-size: clamp(24px, 3vw, 34px);
                letter-spacing: -0.02em;
                line-height: 1.1;
            }

            .subtitle {
                margin: 8px 0 0;
                color: var(--muted);
                font-size: 14px;
            }

            .grid {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 18px;
                align-items: start;
            }
            @media (max-width: 980px) {
                .grid {
                    grid-template-columns: 1fr;
                }
            }

            .card {
                background: linear-gradient(180deg, var(--panel), rgba(255, 255, 255, 0.03));
                border: 1px solid var(--border);
                border-radius: var(--radius);
                box-shadow: var(--shadow);
                overflow: hidden;
                backdrop-filter: blur(10px);
            }

            .card-header {
                padding: 16px 18px;
                display: flex;
                align-items: center;
                justify-content: space-between;
                border-bottom: 1px solid var(--border);
                background: rgba(255, 255, 255, 0.03);
            }

            .card-header h2 {
                margin: 0;
                font-size: 16px;
                letter-spacing: 0.02em;
                color: var(--text);
            }

            .badge {
                font-size: 12px;
                color: var(--muted);
                padding: 6px 10px;
                border: 1px solid var(--border);
                border-radius: 999px;
                background: rgba(255, 255, 255, 0.04);
            }

            .pad {
                padding: 16px;
            }

            .hint {
                margin: 10px 0 0;
                color: var(--muted);
                font-size: 13px;
                line-height: 1.45;
            }

            a.btn {
                display: inline-block;
                text-decoration: none;
                border: 1px solid rgba(255, 255, 255, 0.16);
                background: rgba(255, 255, 255, 0.06);
                color: var(--text);
                padding: 10px 12px;
                border-radius: 12px;
                cursor: pointer;
                transition:
                    transform 0.1s ease,
                    background 0.12s ease,
                    border-color 0.12s ease;
                font-weight: 700;
                letter-spacing: 0.01em;
                white-space: nowrap;
            }
            a.btn:hover {
                transform: translateY(-1px);
                background: rgba(255, 255, 255, 0.1);
                border-color: rgba(255, 255, 255, 0.22);
            }
            a.btn:active {
                transform: translateY(0px);
            }

            .btn-primary {
                background: linear-gradient(180deg, rgba(99, 102, 241, 0.75), rgba(99, 102, 241, 0.35));
                border-color: rgba(99, 102, 241, 0.65);
            }
            .btn-primary:hover {
                background: linear-gradient(180deg, rgba(99, 102, 241, 0.86), rgba(99, 102, 241, 0.42));
                border-color: rgba(99, 102, 241, 0.85);
            }

            .btn-green {
                background: linear-gradient(180deg, rgba(34, 197, 94, 0.62), rgba(34, 197, 94, 0.24));
                border-color: rgba(34, 197, 94, 0.55);
            }
            .btn-green:hover {
                background: linear-gradient(180deg, rgba(34, 197, 94, 0.74), rgba(34, 197, 94, 0.32));
                border-color: rgba(34, 197, 94, 0.75);
            }

            .muted {
                color: var(--muted);
                font-size: 13px;
            }
            .k {
                color: var(--muted2);
                font-size: 11px;
                text-transform: uppercase;
                letter-spacing: 0.06em;
            }
        </style>
    </head>

    <body>
        <div class="container">
            <header>
                <div>
                    <h1>Console</h1>
                    <p class="subtitle">Choose what you want to manage.</p>
                </div>
            </header>

            <div class="grid">
                <section class="card">
                    <div class="card-header">
                        <h2>Videos</h2>
                        <span class="badge">Manage</span>
                    </div>
                    <div class="pad">
                        <div class="muted">Update titles/descriptions, create and delete entries.</div>
                        <a class="btn btn-primary" href="/ui/videos">Go to videos</a>
                    </div>
                </section>

                <section class="card">
                    <div class="card-header">
                        <h2>Users</h2>
                        <span class="badge">View</span>
                    </div>
                    <div class="pad">
                        <div class="muted">See username, role and authorities. Update password.</div>
                        <a class="btn btn-green" href="/ui/users">Go to users</a>
                    </div>
                </section>
            </div>
        </div>
    </body>
</html>
